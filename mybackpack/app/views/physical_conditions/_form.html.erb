<%= form_with(model: physical_condition, local: true) do |form| %>
  <% if physical_condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(physical_condition.errors.count, "error") %> prohibited this physical_condition from being saved:</h2>

      <ul>
      <% physical_condition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
