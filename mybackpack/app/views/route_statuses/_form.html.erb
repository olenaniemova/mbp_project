<%= form_with(model: route_status, local: true) do |form| %>
  <% if route_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route_status.errors.count, "error") %> prohibited this route_status from being saved:</h2>

      <ul>
      <% route_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
