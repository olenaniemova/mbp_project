<%= form_with(model: food_category, local: true) do |form| %>
  <% if food_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_category.errors.count, "error") %> prohibited this food_category from being saved:</h2>

      <ul>
      <% food_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
