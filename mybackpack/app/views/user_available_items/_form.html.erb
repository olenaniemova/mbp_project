<%= form_with(model: user_available_item, local: true) do |form| %>
  <% if user_available_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_available_item.errors.count, "error") %> prohibited this user_available_item from being saved:</h2>

      <ul>
      <% user_available_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :item_id, value: 'Спорядження', class: 'bmd-label-floating' %>
    <%= form.select(:item_id, options_from_collection_for_select(Item.all.sort_by{ |c| c.title }, :id, :title), {}, {:class => 'form-control'})%>
  </div>

  <div class="form-group">
    <%= form.label :count, value: 'Кількість', class: 'bmd-label-floating' %>
    <%= form.number_field :count, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit value: 'Додати', class: 'btn btn-primary btn-raised' %>
  </div>
<% end %>
